import pyodbc

conn_str = "DRIVER={ODBC Driver 17 for SQL Server};SERVER=your_server;DATABASE=your_db;UID=your_user;PWD=your_pwd"
conn = pyodbc.connect(conn_str)
cursor = conn.cursor()

cursor.execute("SELECT TOP 1 * FROM dbo.XLogDocumentProcessor")
row = cursor.fetchone()
print(row)
